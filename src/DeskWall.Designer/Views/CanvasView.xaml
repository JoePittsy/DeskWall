<UserControl x:Class="DeskWall.Designer.Views.CanvasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Focusable="True"
             FocusVisualStyle="{x:Null}"
             Background="{x:Static SystemColors.AppWorkspaceBrush}">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Transparent, not null: a Background is what makes the border hit-testable, and without one
         the right-click never reaches the context menu below. -->
    <Border x:Name="Host" Grid.Row="0" ClipToBounds="True" Background="Transparent">
      <Border.ContextMenu>
        <ContextMenu>
          <MenuItem x:Name="MenuFront" Header="Bring to front" InputGestureText="Ctrl+]" Click="OnBringToFront" />
          <MenuItem x:Name="MenuBack" Header="Send to back" InputGestureText="Ctrl+[" Click="OnSendToBack" />
          <MenuItem x:Name="MenuDuplicate" Header="Duplicate" InputGestureText="Ctrl+D" Click="OnDuplicate" />
          <MenuItem x:Name="MenuDelete" Header="Delete" InputGestureText="Del" Click="OnDelete" />
          <MenuItem x:Name="MenuAlign" Header="Align">
            <MenuItem Header="Left" Tag="Left" Click="OnAlign" />
            <MenuItem Header="Right" Tag="Right" Click="OnAlign" />
            <MenuItem Header="Top" Tag="Top" Click="OnAlign" />
            <MenuItem Header="Bottom" Tag="Bottom" Click="OnAlign" />
            <MenuItem Header="Centre across" Tag="CenterX" Click="OnAlign" />
            <MenuItem Header="Centre down" Tag="CenterY" Click="OnAlign" />
          </MenuItem>
        </ContextMenu>
      </Border.ContextMenu>
    </Border>

    <Border Grid.Row="1" Padding="8,3"
            Background="{x:Static SystemColors.ControlBrush}"
            BorderBrush="{x:Static SystemColors.ControlDarkBrush}"
            BorderThickness="0,1,0,0">
      <TextBlock x:Name="Status" Foreground="{x:Static SystemColors.ControlTextBrush}" />
    </Border>
  </Grid>
</UserControl>
