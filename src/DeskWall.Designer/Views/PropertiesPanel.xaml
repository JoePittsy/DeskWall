<UserControl x:Class="DeskWall.Designer.Views.PropertiesPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}">
    <DockPanel>
        <TextBlock x:Name="EmptyText" DockPanel.Dock="Top" Text="No selection"
                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" Margin="8"/>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="Root" Margin="8"/>
        </ScrollViewer>
    </DockPanel>
</UserControl>
