<Window x:Class="DeskWall.Designer.Views.SourceEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Source"
        Width="420" Height="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="90"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Name" VerticalAlignment="Center"/>
            <TextBox x:Name="NameBox" Grid.Column="1"/>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="90"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Type" VerticalAlignment="Center"/>
            <ComboBox x:Name="TypeCombo" Grid.Column="1" SelectionChanged="TypeCombo_SelectionChanged"/>
            <TextBlock x:Name="TypeText" Grid.Column="1" VerticalAlignment="Center" Visibility="Collapsed"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="90"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Every (s)" VerticalAlignment="Center"/>
            <TextBox x:Name="EveryBox" Grid.Column="1"/>
        </Grid>

        <TextBlock Grid.Row="3" Text="Settings" Margin="0,4,0,4" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>

        <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="SettingsList"/>
        </ScrollViewer>

        <Button Grid.Row="5" x:Name="AddSettingButton" Content="Add setting" HorizontalAlignment="Left" Margin="0,8,0,0" Click="AddSettingButton_Click"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="OkButton" Content="OK" Width="72" Margin="0,0,8,0" IsDefault="True" Click="OkButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="72" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
